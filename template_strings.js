const greeting = 'Hello';
const answer = "Forty Two";

//This is a template string, so we can inject variables into the string
const html = `
    <div>
        ${greeting}
        ${  answer }
    </div>
`;